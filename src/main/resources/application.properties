spring.application.name=VetSecure
<<<<<<< Updated upstream

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true



# Flyway: accept existing tables and create history
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.locations=classpath:db/migration

# Datasource (appuser/apppass from docker-compose)
spring.datasource.url=jdbc:mysql://localhost:3306/vetsecure?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=appuser
spring.datasource.password=apppass
# driver is auto-detected, but harmless if kept:
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=8082
=======
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/vetsecure?useSSL=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Security Headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# SSL Configuration
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${SSL_KEY_STORE_PASSWORD}
server.ssl.key-store-type=PKCS12

# Rate Limiting
rate.limit.enabled=true
rate.limit.requests-per-second=100

# Encryption
encryption.secret=${ENCRYPTION_SECRET}

# Audit Logging
audit.logging.enabled=true
audit.logging.retention-days=90

# JPA/Hibernate Test Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Disable Flyway for Tests
spring.flyway.enabled=false

# JWT Test Configuration
jwt.secret=test-secret-key-0123456789abcdef-0123456789abcd
jwt.accessTtlMillis=60000
jwt.refreshTtlMillis=120000
jwt.mfaTtlMillis=30000
jwt.ttlMillis=60000

# Disable Swagger UI in tests
springdoc.swagger-ui.enabled=false

>>>>>>> Stashed changes
